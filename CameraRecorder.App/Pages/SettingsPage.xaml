<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:CameraRecorder.App.PageModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:DataType="pageModels:SettingsPageModel"
             x:Class="CameraRecorder.App.Pages.SettingsPage"
             x:Name="SettingsPageRoot"
             Title="Настройки">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference SettingsPageRoot}, x:DataType=ContentPage}"
                EventName="Appearing"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <Style x:Key="SectionTitle" TargetType="Label" BasedOn="{StaticResource Title2}">
            <Setter Property="Margin" Value="0,15,0,5" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="Label" BasedOn="{StaticResource Body2}">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>

        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="FontFamily" Value="OpenSansRegular"/>
            <Setter Property="FontSize" Value="{OnIdiom 16,Desktop=24}" />
            <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray500}}" />
            <Setter Property="MinimumHeightRequest" Value="44"/>
            <Setter Property="MinimumWidthRequest" Value="44"/>
        </Style>

        <Style TargetType="Switch">
            <Setter Property="OnColor" Value="{StaticResource Primary}" />
            <Setter Property="VerticalOptions" Value="Center" />
        </Style>

        <Style TargetType="Slider">
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="MinimumTrackColor" Value="{StaticResource Primary}" />
            <Setter Property="MaximumTrackColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray500}}" />
            <Setter Property="ThumbColor" Value="{StaticResource Primary}" />
        </Style>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Сохранить" Command="{Binding SaveCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Spacing="{StaticResource LayoutSpacing}" Padding="{StaticResource LayoutPadding}">

            <!-- ═══ Activity Indicator ═══ -->
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}"
                               HorizontalOptions="Center" Color="{StaticResource Primary}" />

            <!-- ═══ RTSP ═══ -->
            <Label Text="RTSP подключение" Style="{StaticResource SectionTitle}" />

            <Label Text="URL (хост или IP)" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding RtspUrl}" Placeholder="rtsp://192.168.1.8:554/stream1" />

            <Label Text="Логин" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding RtspLogin}" Placeholder="admin" />

            <Label Text="Пароль" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding RtspPassword}" IsPassword="True" Placeholder="••••••" />

            <!-- ═══ Локальное хранилище ═══ -->
            <Label Text="Локальное хранилище" Style="{StaticResource SectionTitle}" />

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Label Text="Локальное хранилище активно" Style="{StaticResource FieldLabel}" Grid.Column="0" />
                <Switch IsToggled="{Binding LocalStorageEnabled}" Grid.Column="1" />
            </Grid>

            <Label Text="Путь для записей" Style="{StaticResource FieldLabel}" />
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Entry Text="{Binding LocalRecordingsPath}" Placeholder="C:\temp\camera\" Grid.Column="0" />
                <Button Text="📁" Command="{Binding PickLocalPathCommand}"
                        Grid.Column="1"
                        MinimumWidthRequest="50" MinimumHeightRequest="44"
                        SemanticProperties.Description="Выбрать папку для записей" />
            </Grid>

            <!-- ═══ FTP ═══ -->
            <Label Text="FTP" Style="{StaticResource SectionTitle}" />

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Label Text="FTP активно" Style="{StaticResource FieldLabel}" Grid.Column="0" />
                <Switch IsToggled="{Binding FtpEnabled}" Grid.Column="1" />
            </Grid>

            <Label Text="Хост" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding FtpHost}" Placeholder="ftp.example.com" />

            <Label Text="Логин" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding FtpLogin}" Placeholder="user" />

            <Label Text="Пароль" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding FtpPassword}" IsPassword="True" Placeholder="••••••" />

            <Label Text="Директория на сервере" Style="{StaticResource FieldLabel}" />
            <Entry Text="{Binding FtpDirectory}" Placeholder="/rec" />

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Label Text="Использовать FTPS (SSL/TLS)" Style="{StaticResource FieldLabel}" Grid.Column="0" />
                <Switch IsToggled="{Binding UseFtps}" Grid.Column="1" />
            </Grid>

            <!-- ═══ Запись ═══ -->
            <Label Text="Параметры записи" Style="{StaticResource SectionTitle}" />

            <Label Style="{StaticResource FieldLabel}">
                <Label.Text>
                    <Binding Path="PreMotionDurationSec" StringFormat="До движения: {0} сек" />
                </Label.Text>
            </Label>
            <Slider Value="{Binding PreMotionDurationSec}" Minimum="0" Maximum="60" />

            <Label Style="{StaticResource FieldLabel}">
                <Label.Text>
                    <Binding Path="PostMotionDurationSec" StringFormat="После движения: {0} сек" />
                </Label.Text>
            </Label>
            <Slider Value="{Binding PostMotionDurationSec}" Minimum="0" Maximum="60" />

            <!-- ═══ Save Button ═══ -->
            <Button Text="Сохранить настройки"
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsSaving, Converter={StaticResource InvertedBoolConverter}}"
                    SemanticProperties.Description="Сохранить настройки"
                    MinimumHeightRequest="{OnIdiom 44,Desktop=60}"
                    Margin="0,20,0,30" />

            <ActivityIndicator IsRunning="{Binding IsSaving}" IsVisible="{Binding IsSaving}"
                               HorizontalOptions="Center" Color="{StaticResource Primary}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
